# Known VITE_-prefixed secrets shipped to the browser.
#
# Anything listed here is PUBLIC to anyone who loads the app. Listing a
# variable does not make it safe — it records an accepted, outstanding risk so
# that check-vite-env.sh can still fail the build on anything NEW.
#
# This file is intentionally EMPTY.
#
# It would previously have listed VITE_TURSO_AUTH_TOKEN: the clinical sessions
# database credential, inlined into every build and carrying full
# read/write/delete over the whole clinical_sessions table. Turso now sits
# behind the Entra-authenticated epsa-turso-proxy Worker and the browser holds
# no database credential at all. ROTATE the old token — it was public in every
# deployed bundle.
#
# The remaining VITE_ vars are public by design: Azure client/tenant IDs and
# Worker URLs.
